<?xml version="1.0" encoding="UTF-8"?>
<entity-mappings
        xmlns="https://jakarta.ee/xml/ns/persistence/orm"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence/orm
                            https://jakarta.ee/xml/ns/persistence/orm/orm_3_1.xsd"
        version="3.1">

    <!--
        Yenileme token'ı varlığı.
        BaseEntity'den türemez; kendi @Id alanını doğrudan tanımlar.
        token: benzersiz (unique="true") — tekrar kullanım önleme.
        isRevoked: token iptal durumu — false ile başlar, revoke() ile true'ya döner.
    -->
    <entity class="tr.com.huseyinaydin.domain.token.RefreshToken" access="FIELD">

        <table name="REFRESH_TOKENS"/>

        <attributes>
            <id name="id">
                <column name="ID"/>
            </id>
            <basic name="userId">
                <column name="USER_ID" nullable="false"/>
            </basic>
            <basic name="token">
                <column name="TOKEN" nullable="false" unique="true" length="512"/>
            </basic>
            <basic name="expiration">
                <column name="EXPIRATION" nullable="false"/>
            </basic>
            <basic name="isRevoked">
                <column name="IS_REVOKED" nullable="false"/>
            </basic>
        </attributes>

    </entity>

</entity-mappings>
