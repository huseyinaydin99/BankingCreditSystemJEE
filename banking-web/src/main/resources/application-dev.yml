spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    tr.com.huseyinaydin: DEBUG
    org.hibernate.SQL: DEBUG

cors:
  allowed-origins: "*"
